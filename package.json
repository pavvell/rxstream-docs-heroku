{
  "name": "rxstream-docs-heroku",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "git push heroku master",
    "rebuild": "git commit -am 'rebuild' --allow-empty; npm run deploy;"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "4.15.3",
    "rxstream": "*"
  }
}
